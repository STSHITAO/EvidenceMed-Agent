# Reference Images

将用于混合模态检索的医学参考影像放在这个目录下。

推荐做法：

- 图像文件：`case_001.png`
- 同名描述文件：`case_001.txt`

其中 `.txt` 或 `.md` 文件可写入影像描述、病种标签、报告摘要或指南关键词。
入库时系统会将“图像 + 描述文本”作为一个混合模态知识项写入 Milvus。

如果没有 sidecar 文本，系统会退化为使用文件名和目录名生成简短描述。
